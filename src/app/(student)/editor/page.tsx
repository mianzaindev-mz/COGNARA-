import { PortalStub } from "@/components/shared/portal-stub";

export default function EditorPage() {
  return (
    <PortalStub
      title="Code editor"
      description="Monaco + Judge0 integration arrives in the compiler milestone."
    />
  );
}
