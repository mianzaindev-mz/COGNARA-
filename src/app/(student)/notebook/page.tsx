import { PortalStub } from "@/components/shared/portal-stub";

export default function NotebookPage() {
  return (
    <PortalStub
      title="Notebook"
      description="TipTap + Tldraw notebooks with AI note-taking hooks."
    />
  );
}
