import { PortalStub } from "@/components/shared/portal-stub";

export default function SettingsPage() {
  return (
    <PortalStub
      title="Settings"
      description="Theme, accessibility, notifications, and cookie preferences."
    />
  );
}
