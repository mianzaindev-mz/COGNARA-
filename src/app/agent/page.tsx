import { PortalStub } from "@/components/shared/portal-stub";

export default function AgentPage() {
  return (
    <PortalStub
      title="COGNARA agent"
      description="Full multi-skill agent UI, credits, and tool execution land in Day 5."
    />
  );
}
